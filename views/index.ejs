<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>We build You</title>
    <style>
        .hero-section {
            position: relative;
            text-align: center;
            color: white;
            height: 100vh;
            background: url('https://t4.ftcdn.net/jpg/07/32/93/57/360_F_732935712_bFYjXa4vSrhMCOf47wutlUdFaLtuegy4.jpg') no-repeat center center/cover;
        }
        .hero-text {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
        }
        .hero-text h1 {
            font-size: 4em;
            font-weight: bold;
            margin: 0;
        }
        .hero-text button {
            background-color: purple;
            border: none;
            color: white;
            padding: 15px 32px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 1em;
            margin-top: 20px;
            cursor: pointer;
        }
        .info-section {
            padding: 50px 0;
            text-align: center;
            background-color: #f8f9fa;
        }
        .info-card {
            box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);
            padding: 16px;
            background-color: white;
            margin: 20px;
        }
        .info-card h3 {
            color: red;
            margin-bottom: 16px;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
    <div class="hero-section">
        <div class="hero-text">
            <h1>Muscle growth made simple</h1>
            <button onclick="window.location.href='/createplan'">Try it out!</button>
        </div>
    </div>
</body>
</html>